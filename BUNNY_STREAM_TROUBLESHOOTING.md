# Bunny Stream Troubleshooting Guide

## Помилка: "Video error: {}"

Це означає, що є проблема з підключенням до Bunny Stream. Ось як це виправити:

## ✅ Перевірка налаштувань

### 1. Environment Variables

Переконайтеся, що у вас є файл `.env.local` з правильними значеннями:

```bash
NEXT_PUBLIC_BUNNY_STREAM_LIBRARY_ID=your-library-id
NEXT_PUBLIC_BUNNY_STREAM_API_KEY=your-api-key
```

### 2. Перевірка Library ID та API Key

Зайдіть в Bunny Stream Dashboard:
1. Перейдіть в **My Stream** → **Video Library**
2. Натисніть на ваш Library
3. Скопіюйте **Library ID**
4. Натисніть **API Key** та скопіюйте ключ

### 3. Перезапустіть dev server

Після змінених environment variables:
```bash
# Зупиніть сервер (Ctrl+C)
npm run dev
```

## 🔍 Причини помилок

### 1. Відео не існує

Якщо ви додали пустий videoId, переконайтеся що:
- Відео завантажено в Bunny Stream
- Ви маєте правильний videoId

### 2. Відео не закодовано

Після завантаження відео потребує кодування. Перевірте статус в Dashboard:
- **Status**: Finished - відео готове
- **Status**: Processing - ще кодується

### 3. CORS помилка

Якщо бачите CORS помилку в console:
1. Зайдіть в Bunny Stream Dashboard
2. Settings → CORS Domains
3. Додайте ваш domain: `localhost:3000`

## 🧪 Тестування підключення

### Перевірка через API

Відкрийте в браузері:
```
http://localhost:3000/api/bunny-stream/videos
```

Має бути JSON зі списком відео або порожній масив.

### Перевірка в Console

Відкрийте DevTools (F12) і перевірте:
- Чи є вкладка **Console** з помилками
- Network tab покаже чи завантажуються відео файли

## 📝 Покрокова інструкція

### Крок 1: Створіть Video Library

1. Зайдіть на https://bunny.net
2. Створіть акаунт (якщо немає)
3. My Stream → Create Library
4. Введіть назву та збережіть

### Крок 2: Отримайте ключі

1. Відкрийте ваш Library
2. Settings → скопіюйте Library ID
3. Settings → API Key → скопіюйте ключ

### Крок 3: Додайте environment variables

Створіть `.env.local` в корені проекту:
```bash
NEXT_PUBLIC_BUNNY_STREAM_LIBRARY_ID=XXXXXX-XXXX-XXXX
NEXT_PUBLIC_BUNNY_STREAM_API_KEY=XXXXXX
```

### Крок 4: Завантажте тестове відео

1. В Bunny Stream Dashboard
2. Натисніть **Upload Video**
3. Виберіть файл (макс 100MB на безкоштовному плані)
4. Зачекайте завершення кодування

### Крок 5: Отримайте Video ID

1. В Dashboard клікніть на завантажене відео
2. Скопіюйте **Video ID** з URL

### Крок 6: Тестуйте плеєр

На сторінці `/video-example` введіть Video ID та натисніть "Load Video"

## 🚨 Поширені помилки

### "Fetch failed"

**Причина**: Неправильний API ключ або Library ID

**Рішення**: Перевірте що всі ключі правильні в `.env.local`

### "Video load failed"

**Причина**: Відео ще не закодовано або не існує

**Рішення**: Зачекайте завершення кодування в Dashboard

### CORS error

**Причина**: Ваш domain не додано в CORS налаштування

**Рішення**: 
1. Dashboard → Settings → CORS Domains
2. Додайте `localhost:3000`

### Пустий video error

**Причина**: Не вказано videoId

**Рішення**: Введіть правильний Video ID

## 📞 Додаткова допомога

Якщо проблема не вирішена:

1. Перевірте Console на помилки
2. Перевірте Network tab чи завантажуються файли
3. Перевірте що відео Finished в Dashboard
4. Спробуйте відкрити URL відео напряму в браузері

### Приклад URL:

```
https://vz-XXXXXX.b-cdn.net/VIDEO_ID/play.mp4
```

Замініть XXXXXX на ваш Library ID та VIDEO_ID на ID відео.

---

**Переконайтеся що:**
- ✅ Environment variables додано
- ✅ Dev server перезапущено
- ✅ Відео закодовано (Status: Finished)
- ✅ Ви маєте правильний Video ID


